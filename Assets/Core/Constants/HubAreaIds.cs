namespace Core.Constants
{
    /// <summary>
    /// Hub zone ids (matches hub save / unlock flow). Add new zones as consts here.
    /// </summary>
    public static class HubAreaIds
    {
        public const string MainConcourse = "mq.hub.main_concourse";
    }
}
